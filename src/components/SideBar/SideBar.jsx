import React from 'react';

import './SideBar.sass';
import Header from './helpers/Header/Header';

const SideBar = () => (
  <div class="side-bar">
    <Header />
  </div>
);

export default SideBar;
